import { User } from "@/types/users";

export const ApplicantProfileForm = ({ user }: { user: User }) => {
  return (
    <div className="bg-card rounded-xl border border-dashed p-12 text-center">
      <h2 className="mb-2 text-xl font-bold">Resume Builder</h2>
    </div>
  );
};
